# Experiment Notes

Store one markdown file per experiment assignment in this directory.

Start from `claude/templates/experiment-task.md` when using the Claude Code
workflow, or `codex/templates/experiment-task.md` when using the older Codex
flow, so every worker gets the same metadata, run plan, and result fields.
